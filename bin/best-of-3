#!/usr/bin/env bash
# Interactive best-buildable-deck picker, preset to Bo3 (skips the Bo prompt).
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")/.." && pwd)"
exec "$REPO_ROOT/.venv/bin/mtga-mcp" deck wizard --best-of 3 "$@"
